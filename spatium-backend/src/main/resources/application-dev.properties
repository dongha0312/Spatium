######################################
## dev 프로필 전용 설정 ##
##  - spring.profiles.active=dev일 때만 적용됨
##  - 운영(prod) 배포 시에는 SPRING_PROFILES_ACTIVE를 dev가 아닌 값으로
##    설정해야 하며, 그 경우 이 파일의 값들은 적용되지 않음
######################################

# 로컬 개발용 JWT 서명 키 기본값
#  - JWT_SECRET 환경변수가 있으면 그 값을 우선 사용
#  - 이 키는 git에 공개된 '로컬 전용' 더미 키다. 운영에서는 절대 사용 금지이며,
#    운영은 반드시 JWT_SECRET 환경변수로 별도의 비밀 키를 주입해야 한다.
#  - 이전에 공개돼 있던 키(...change-me-32bytes)는 유출된 것으로 간주하고 교체함.
spatium.jwt.secret=${JWT_SECRET:local-only-dev-signing-key-do-not-use-in-prod-0007}

# 개발용 DB 접속정보 기본값 (환경변수가 있으면 그 값을 우선 사용)
#  - (주의) 이 값들은 git에 공개되어 있음. 팀 결정으로 개발 편의를 위해 유지하는
#    것이며, 운영 DB에는 반드시 다른 계정/비밀번호를 사용할 것
spring.datasource.url=${DB_URL:jdbc:oracle:thin:@210.119.12.115:1521/xe}
spring.datasource.username=${DB_USERNAME:spatium}
spring.datasource.password=${DB_PASSWORD:dbdb}

# 개발 시에만 실행 SQL을 콘솔에 출력
spring.jpa.show-sql=true

# 코드 변경 시 자동 재시작 (개발 전용)
spring.devtools.restart.enabled=true
# 정적 파일(css/js/html) 자동 리로드 (개발 전용)
spring.devtools.livereload.enabled=true
